# Copy to backend/.env and set real values (do NOT commit)
# PostgreSQL connection
DATABASE_URL=jdbc:postgresql://localhost:5432/urfa_grill
DATABASE_USER=urfa
DATABASE_PASSWORD=replace-with-secure-password

# JWT secret (use a long random value)
JWT_SECRET=replace-with-long-random-secret
